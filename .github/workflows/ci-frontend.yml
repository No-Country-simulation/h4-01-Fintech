on:
  push:
    branches:
      - "develop"

jobs:
  CI-FrontEnd-Deploy-Render:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      
      - name: List files in the repository
        run: ls -alh

      - name: Install dependencies
        run: |
          cd front/
          npm install

      - name: Deploy
        if: github.ref == 'refs/heads/main'
        env:
          deploy_url: ${{ secrets.RENDER_DEPLOY_HOOK_URL_FRONT }}
        run: |
          curl "$deploy_url"